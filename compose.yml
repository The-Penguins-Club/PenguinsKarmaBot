services:
  penguinskarmabot:
    build:
      context: .
    restart: unless-stopped
    env_file:
      - config.env
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./PenguinsKarma.db:/app/PenguinsKarma.db
